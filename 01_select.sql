-- tous les champs de la table actor
select * from actor;

-- noms et prénoms de tous les acteurs
select first_name, last_name from actor;

-- tous les champs de la table film
select * from film;

-- titre et année de sortie de tous les films
select title, release_year from film;
